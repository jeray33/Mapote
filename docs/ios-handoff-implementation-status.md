# iOS 交接实现状态清单（最终交付勾选矩阵）

> 基于 `ios-handoff-document.md` 的 1~16 章节逐条映射。  
> 勾选定义：`[x] 已完成` / `[-] 部分完成` / `[ ] 待补齐`

## 章节总览矩阵

| 章节 | 模块 | 状态 | 备注 |
|---|---|---|---|
| 1 | 应用概述 | `[-]` | 核心流程可用，地图 SDK 深度能力仍有差异 |
| 2 | 数据模型 | `[x]` | 核心模型已落地 |
| 3 | 数据持久化 | `[x]` | 本地存储与迁移已实现 |
| 4 | Markdown 规范 | `[-]` | 核心语法支持，复杂嵌套规则待增强 |
| 5 | 页面与导航 | `[x]` | 首页/编辑器双页面状态已实现 |
| 6 | 首页笔记列表 | `[x]` | 新建、卡片、二次删除确认已实现 |
| 7 | 编辑模式 | `[-]` | 核心可用，未 1:1 复刻 Tiptap 全部能力 |
| 8 | 列表模式 | `[x]` | 分段、路线、重排、备注编辑均可用 |
| 9 | 地图模式 | `[-]` | MapKit 主渲染，Google/高德原生差异未完全对齐 |
| 10 | 地图引擎抽象层 | `[-]` | 抽象与回退有，真实双 SDK 实现待补 |
| 11 | AI 文字识别导入 | `[x]` | 提取、过滤、匹配、导入流程可用 |
| 12 | AI 旅行助手聊天 | `[x]` | 流式、分组解析、单点/整组添加、持久化可用 |
| 13 | API 缓存层 | `[x]` | 双层缓存与 TTL 已实现 |
| 14 | 地点分类系统 | `[x]` | 分类映射与视觉应用已落地 |
| 15 | UI 设计规范 | `[-]` | 主要规范已覆盖，细节动画与样式仍可继续打磨 |
| 16 | iOS 迁移建议 | `[x]` | 当前实现已采用文档建议主路径 |

## 逐章勾选清单（1~16）

### 1. 应用概述
- [x] 旅行路线笔记主流程（编辑 / 列表 / 地图）可用
- [x] `@` 搜索插入地点标签可用
- [x] AI 导入与 AI 聊天可用
- [-] 双地图引擎能力存在（抽象+切换+回退），但非真实双 SDK 1:1 复刻

### 2. 数据模型
- [x] `PlaceCategory` 与分类关键词映射
- [x] `Place`、`RouteInfo`、`TravelMode`、`Note`、`ViewMode`
- [x] 地图与 AI 相关结构（`MapPlace`、`SearchOptions`、`ChatMessage` 等）

### 3. 数据持久化
- [x] 笔记持久化（`place-notes`）
- [x] 地图设置与引擎持久化（`map-settings` / `map-engine-type`）
- [x] AI 聊天历史持久化（`ai_chat_{noteId}`）
- [x] 旧版 `blocks -> markdown` 迁移逻辑

### 4. Markdown 格式规范
- [x] `::place[name]{#id}` 解析/提取/替换
- [x] H1 分段解析与按段展示
- [x] 地点间文字提取为上下文备注
- [x] 按 markdown 顺序提取有序地点
- [-] 复杂 Markdown 嵌套与序列化细节仍可继续增强

### 5. 页面与导航
- [x] 首页笔记列表 + 编辑器双状态导航
- [x] 编辑器三模式切换（NOTE / LIST / MAP）

### 6. 首页笔记列表
- [x] 顶栏与新建入口
- [x] 笔记卡片信息展示
- [x] 两步删除确认
- [x] 新建后直接进入编辑

### 7. 编辑模式（EditMode）
- [x] 标题编辑、锁定/解锁、导入、AI 转换按钮
- [x] `UITextView + NSTextAttachment` 地点芯片渲染
- [x] `@` 触发规则（前置字符校验、50 字符窗口、光标精确替换）
- [x] 工具栏结构化命令（粗体/标题/列表/任务/分隔线）
- [x] 地点详情弹窗、删除地点、导航
- [-] 未完全复刻 Tiptap 生态与全部富文本行为

### 8. 列表模式（ListView）
- [x] 分段标签页与当前段筛选
- [x] 地点卡片展开详情
- [x] 路线懒加载 + 交通方式循环切换
- [x] 智能排序（最近邻）
- [x] 重排能力（拖拽模式 + 快捷上/下移）
- [x] 总路程/总耗时汇总
- [x] 备注编辑（保存/取消）+ 上下文备注与手动备注合并展示

### 9. 地图模式（MapView）
- [x] 地图标记、分段筛选、Fit Bounds
- [x] 导航路线与地点连线显示开关
- [x] 地图设置面板（主题、图层/标注、POI 分类 8 开关）
- [x] 地点卡片（轮播、评分、建议时长、营业时间、导航）
- [-] 目前以 MapKit 呈现，Google/高德渲染细节未完全对齐

### 10. 地图引擎抽象层
- [x] `MapEngine` 抽象接口与 `MapEngineType`
- [x] 搜索/详情/路线统一调用入口
- [x] 引擎初始化、切换、失败回退与错误提示
- [x] 坐标转换算法文件（`CoordTransform`）
- [-] Google Maps iOS SDK / 高德 iOS SDK 的真实双实现仍待接入

### 11. AI 功能 — 文字识别导入
- [x] Gemini 提取地点
- [x] broad_area 过滤逻辑
- [x] 搜索与回退策略
- [x] 一键转换（markdown 内替换地点标签）
- [x] 导入对话框（识别、勾选、批量加入）

### 12. AI 功能 — 旅行助手聊天
- [x] 底部 Sheet 聊天面板
- [x] 流式响应解析（SSE）
- [x] `json:places` 代码块解析为分组卡片
- [x] 单点添加 / 整组添加
- [x] 对话持久化
- [x] 快捷提问
- [x] 缩略图异步加载 + 内存缓存
- [x] 整组添加进度与失败统计

### 13. API 缓存层
- [x] 内存缓存 + 持久缓存双层策略
- [x] TTL（内存 30 分钟、持久 24 小时）
- [x] 缓存读取顺序与回填

### 14. 地点分类系统
- [x] 分类关键词识别与默认回退
- [x] 分类颜色、图标、emoji 统一映射
- [x] 编辑/列表/地图多处视觉一致性使用

### 15. UI 设计规范
- [x] 主要配色、圆角、组件风格与信息层级落地
- [x] 移动端触控尺寸与底部交互区适配
- [x] 关键动画/反馈（按钮缩放、加载态、sheet 展示）
- [-] 与原 Web 视觉样式仍存在细节差异，可继续精修

### 16. iOS 迁移建议
- [x] 技术栈路径采用 SwiftUI + 本地存储 + URLSession
- [x] 编辑器采用 `UITextView + Attachment` 方案
- [x] 地图层采用 iOS 原生路线（当前为 MapKit 主路径）
- [x] AI 集成采用 REST + 流式解析路径

## 最终交付结论

- 已完成章节：`2, 3, 5, 6, 8, 11, 12, 13, 14, 16`
- 部分完成章节：`1, 4, 7, 9, 10, 15`
- 主要未闭环点：真实 `Google Maps iOS SDK / 高德 iOS SDK` 双引擎深接与细节对齐

