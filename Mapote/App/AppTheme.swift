import SwiftUI

enum AppTheme {
    static let background = Color.white
    static let paper = Color.white
    static let card = Color.white
    static let foreground = Color(hue: 220 / 360, saturation: 0.2, brightness: 0.16)
    static let foregroundSoft = Color(hue: 215 / 360, saturation: 0.06, brightness: 0.42)
    static let primary = Color(hue: 210 / 360, saturation: 0.64, brightness: 0.48)
    static let secondary = Color(hue: 210 / 360, saturation: 0.06, brightness: 0.985)
    static let muted = Color(hue: 210 / 360, saturation: 0.04, brightness: 0.965)
    static let accent = Color(hue: 199 / 360, saturation: 0.72, brightness: 0.5)
    static let destructive = Color(hue: 0, saturation: 0.72, brightness: 0.51)
    static let border = Color(hue: 214 / 360, saturation: 0.04, brightness: 0.93)
    static let shadow = Color.black.opacity(0.035)
}

