import Foundation

// Deprecated: all editor interactions (tap, swipe-to-select, long-press drag,
// group drag, drop indicator) now live in the WKWebView (JS gesture pipeline
// in editor-web/src/Editor.tsx). This file is intentionally left empty to
// avoid mutating the Xcode project file; remove it from the project target
// at your convenience.
